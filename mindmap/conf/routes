GET     /assets/*file               controllers.Assets.at(path="/public", file)

# GET     /                           controllers.Application.index()
# POST    /                           controllers.Application.create()
# GET     /:id                        controllers.Application.show(id: String, vs: ViewSettings)
# GET     /:id/control                controllers.Application.update(id: String)

GET     /v1                         controllers.V1.index()
GET     /v1/:id                     controllers.V1.show(id: String, vs: ViewSettings)
GET     /v2                         controllers.V2.index()
GET     /v2/:id                     controllers.V2.show(id: String, vs: ViewSettings)
GET     /v3                         controllers.V3.index()
GET     /v3/:id                     controllers.V3.show(id: String, vs: ViewSettings)
